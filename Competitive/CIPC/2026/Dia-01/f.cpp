#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef string str;
#define vec vector

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, u, v;
        cin >> n >> u >> v;

        vec <int> malos (n);
        for (int i = 0; i < n; i++) cin >> malos[i];


        int izq = malos[0];
        int der = malos[n - 1];


        //(formato y, x)
    }
    return 0;
}
