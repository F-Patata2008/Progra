#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define str string
#define vec vector

int main() {
    int ini, vaca;
    cin >> ini >> vaca;

    int pos,ubi, suma;
    suma = 0;
    pos = ini;
    int i= 1;
    if (pos)

    while (pos != vaca) {
        pos += i;
        if (pos==vaca) {

        }
    }

    return 0;
}
