#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef string str;
#define vec vector

vec<int> a;


int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        a.assign(n, -1);
        for (int i = 0; i < n; i++) cin >> a[i];


    }
    return 0;
}
