#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef string str;
#define vec vector

int main() {
    int n;
    cin >> n;

    map<int, int> a;

    for (int i = 2; i < 2 * n + 1; i++) a[i]=i;


    cout << 1 << endl;

    int r;
    while (1) {
        cin >> r;
        if (!r)
            return 0;
        else {
            if ()
            cout << endl;
        }
    }

    return 0;
}
